package proxy;

public interface MathCalculator {
    double add(double first, double second);

    double suv(double first, double second);

    double mul(double first, double second) throws Exception;

    double div(double first, double second);
}
